# DeltaPolygon
Library for efficient storage of space-time polygons using deltas. Implements a model that separates the immutable topological structure from the temporal states of vertices, enabling ultra-efficient storage through relative deltas, vertex clustering, and temporal functions.
